import Link from "next/link";
import { notFound } from "next/navigation";
const pages = { privacy: { title: "Privacy", body: "LaunchLens processes the public URL you submit to generate an automated website-health report. We store the URL, normalized results and any generated screenshot for 30 days, after which they are deleted. We do not require accounts and do not sell personal data. Do not submit private, internal, password-protected, or sensitive URLs." }, terms: { title: "Terms", body: "LaunchLens provides automated technical guidance for informational purposes. It does not guarantee search rankings, legal accessibility compliance, security, availability, or business outcomes. You may audit only public websites you are authorised to assess. Reports are available for 30 days and may be removed to protect service reliability." } };
export default async function LegalPage({ params }: { params: Promise<{ page: string }> }) { const { page } = await params; const content = pages[page as keyof typeof pages]; if (!content) notFound(); return <main className="empty-state shell"><Link href="/" className="back-link">← Back to LaunchLens</Link><p className="kicker">LaunchLens</p><h1>{content.title}</h1><p>{content.body}</p></main>; }
